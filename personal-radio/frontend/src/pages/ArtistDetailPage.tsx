import { useEffect,useState } from 'react'
import Artwork from '../components/Artwork'
import LoadingSkeleton from '../components/LoadingSkeleton'
import PageError from '../components/PageError'
import { getAlbumQueue,getArtistDetail,getArtistQueue,mediaUrl,type AlbumSummary,type ArtistDetail,type Track } from '../api'
import { usePlayback } from '../state/PlaybackContext'
import { trackToNowPlaying } from '../utils/mediaMappers'
export default function ArtistDetailPage({artist,onBack,onOpenAlbum}:{artist:string;onBack:()=>void;onOpenAlbum:(a:AlbumSummary)=>void}){const [detail,setDetail]=useState<ArtistDetail|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null);const {playQueue}=usePlayback();const load=()=>{setLoading(true);setError(null);getArtistDetail(artist).then(setDetail).catch(()=>setError('Could not load artist.')).finally(()=>setLoading(false))};useEffect(load,[artist]);if(loading)return <LoadingSkeleton rows={6}/>;if(error||!detail)return <PageError message={error??'Artist not found.'} onRetry={load}/>;const play=(tracks:Track[],i=0)=>playQueue(tracks.map(t=>trackToNowPlaying(t)),i);return <div><button onClick={onBack} style={{display:'flex',alignItems:'center',gap:6,padding:'10px 0',marginBottom:16,minHeight:44,color:'var(--text-secondary)',fontSize:14,fontWeight:500}}>&larr; Library</button><div style={{marginBottom:20}}><h1 style={{fontSize:'2rem',fontWeight:800,letterSpacing:'-0.03em',marginBottom:4}}>{detail.name}</h1><p style={{fontSize:13,color:'var(--text-muted)'}}>{detail.track_count} {detail.track_count===1?'track':'tracks'} {'\u00b7'} {detail.album_count} {detail.album_count===1?'album':'albums'}</p><div style={{display:'flex',gap:8,marginTop:14}}><button onClick={()=>void getArtistQueue(detail.name).then(r=>play(r.queue))} style={{padding:'10px 14px',borderRadius:'var(--radius-pill)',background:'var(--accent-primary)',color:'#fff',fontWeight:700}}>Play Artist Radio</button><button onClick={()=>play(detail.tracks)} style={{padding:'10px 14px',borderRadius:'var(--radius-pill)',background:'var(--bg-surface)',color:'var(--text-primary)',fontWeight:700}}>Play All</button></div></div><p className="section-label">Albums</p><div style={{display:'grid',gap:10,marginBottom:24}}>{detail.albums.map(a=><div key={a.title} className="card-premium" style={{padding:'10px 14px',display:'flex',gap:12,alignItems:'center'}}><button onClick={()=>onOpenAlbum(a)} style={{display:'flex',gap:12,alignItems:'center',flex:1,minWidth:0,textAlign:'left'}}><Artwork src={mediaUrl('/api/media/albums/cover?artist='+encodeURIComponent(a.artist)+'&album='+encodeURIComponent(a.title))} label={a.title} size={52}/><span style={{minWidth:0}}><strong style={{display:'block',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{a.title}</strong><span style={{fontSize:12,color:'var(--text-muted)'}}>{a.year??''} {a.year?'\u00b7 ':''}{a.track_count} {a.track_count===1?'track':'tracks'}</span></span></button><button onClick={()=>void getAlbumQueue(a.artist,a.title).then(r=>play(r.queue))} style={{padding:'8px 12px',borderRadius:'var(--radius-pill)',background:'var(--accent-primary)',color:'#fff',fontSize:12,fontWeight:700}}>Play</button></div>)}</div><p className="section-label">Songs</p><div style={{display:'grid',gap:7}}>{detail.tracks.slice(0,80).map((t,i)=><button key={t.id} onClick={()=>play(detail.tracks,i)} className="card-premium" style={{padding:12,textAlign:'left'}}><strong style={{display:'block',fontSize:14}}>{t.title}</strong><span style={{fontSize:12,color:'var(--text-muted)'}}>{t.album}</span></button>)}</div></div>}