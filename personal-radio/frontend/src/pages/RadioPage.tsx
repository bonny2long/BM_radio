import { useEffect,useMemo,useState } from 'react'
import Artwork from '../components/Artwork'
import IconButton from '../components/IconButton'
import LoadingSkeleton from '../components/LoadingSkeleton'
import PageError from '../components/PageError'
import { PlayIcon } from '../components/PlayerIcons'
import { getStationQueue,getStations,type Station } from '../api'
import { usePlayback } from '../state/PlaybackContext'
import { trackToNowPlaying } from '../utils/mediaMappers'
const groups=[['Featured',['recently_added','favorites','deep_cuts']],['Genres',['genre']],['Artists',['artist']]] as const
export default function RadioPage(){const [stations,setStations]=useState<Station[]>([]),[busy,setBusy]=useState<string|null>(null),[loading,setLoading]=useState(true),[pageError,setPageError]=useState<string|null>(null);const {playQueue}=usePlayback();const loadStations=()=>{setLoading(true);setPageError(null);getStations().then(setStations).catch(()=>setPageError('Could not load stations. Check your NAS connection.')).finally(()=>setLoading(false))};useEffect(loadStations,[]);const play=async(s:Station)=>{setBusy(s.name);try{const seed=s.seed_value??((s.type==='genre'||s.type==='artist')?s.name.replace(/ Radio$/,''):null);playQueue((await getStationQueue(s.type,seed)).queue.map(t=>trackToNowPlaying(t,{stationName:s.name})))}finally{setBusy(null)}};const featured=useMemo(()=>stations.find(s=>s.type==='recently_added')??stations[0],[stations]);if(loading)return <LoadingSkeleton rows={6}/>;if(pageError)return <PageError message={pageError} onRetry={loadStations}/>;return <div style={{width:'100%',maxWidth:'100%',minWidth:0,overflowX:'hidden'}}><div style={{marginBottom:18}}><h1 style={{fontSize:'1.6rem',fontWeight:800,letterSpacing:'-0.03em'}}>Stations</h1></div>{featured&&<button onClick={()=>void play(featured)} disabled={!!busy} style={{width:'100%',padding:'18px 20px 18px 24px',borderRadius:'var(--radius-card)',background:'var(--bg-card)',border:'1px solid var(--border-subtle)',textAlign:'left',color:'var(--text-primary)',marginBottom:24,boxShadow:'var(--shadow-card)',display:'flex',alignItems:'center',gap:16,position:'relative',overflow:'hidden'}}><div style={{position:'absolute',left:0,top:0,bottom:0,width:4,background:'var(--gradient-radio)',borderRadius:'4px 0 0 4px'}}/><svg aria-hidden="true" width="90" height="48" viewBox="0 0 90 48" style={{position:'absolute',right:68,top:'50%',transform:'translateY(-50%)',opacity:.08,pointerEvents:'none'}}>{[4,10,18,6,24,14,8,20,12,16,6,22].map((h,i)=><rect key={i} x={i*8} y={(48-h)/2} width={4} height={h} rx={2} fill="var(--accent-primary)"/>)}</svg><div style={{flex:1,minWidth:0}}><p style={{fontSize:10,fontWeight:700,letterSpacing:'0.12em',textTransform:'uppercase',color:'var(--accent-primary)',marginBottom:6,opacity:.85}}>On Air Now</p><p style={{fontSize:20,fontWeight:800,letterSpacing:'-0.02em',color:'var(--text-primary)',marginBottom:4,lineHeight:1.1,whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{featured.name}</p><p style={{fontSize:12,color:'var(--text-muted)'}}>{busy===featured.name?'Building queue...':`${featured.track_count} ${featured.track_count===1?'track':'tracks'} ready`}</p></div><div style={{width:44,height:44,borderRadius:'50%',flexShrink:0,background:'var(--accent-primary)',display:'grid',placeItems:'center',boxShadow:'0 4px 16px var(--accent-primary-glow)'}}><svg width="16" height="16" viewBox="0 0 24 24" fill="white"><polygon points="5,3 19,12 5,21"/></svg></div></button>}{groups.map(([title,types])=>{const list=stations.filter(s=>types.includes(s.type as never)&&s!==featured);if(!list.length)return null;return <section key={title} style={{marginBottom:26}}><p className="section-label">{title}</p><div style={{display:'grid',gap:9}}>{list.map(s=><div className="card-premium" style={{padding:12,display:'grid',gridTemplateColumns:'48px minmax(0,1fr) 38px',alignItems:'center',gap:12,width:'100%',maxWidth:'100%',minWidth:0,overflow:'hidden'}} key={s.name}><Artwork label={s.name} kind="station" size={48}/><div style={{flex:1,minWidth:0}}><strong style={{display:'block',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{s.name}</strong><span style={{fontSize:12,color:'var(--text-muted)'}}>{s.track_count} {s.track_count===1?'track':'tracks'}</span></div><IconButton label={'Play '+s.name} onClick={()=>void play(s)} active={busy===s.name} disabled={!!busy&&busy!==s.name} size={38}><PlayIcon/></IconButton></div>)}</div></section>})}</div>}
