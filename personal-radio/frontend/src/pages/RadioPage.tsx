import { useEffect, useState } from 'react'
import { getStations, type Station } from '../api'
export default function RadioPage() { const [stations, setStations] = useState<Station[]>([]); useEffect(() => { void getStations().then(setStations).catch(() => {}) }, []); return <div><div style={{ marginBottom: 28 }}><h1 style={{ fontSize: '2rem', fontWeight: 800 }}>Stations</h1><p style={{ color: 'var(--text-muted)', fontSize: 13 }}>Auto-generated from your library</p></div>{stations.length ? <div style={{ display: 'grid', gap: 10 }}>{stations.map((station) => <div className="card-premium" style={{ padding: 16 }} key={station.name}><strong>{station.name}</strong><div style={{ color: 'var(--text-muted)', fontSize: 12, marginTop: 3 }}>{station.type} · {station.track_count} tracks</div></div>)}</div> : <div className="card-premium" style={{ padding: 40, textAlign: 'center', color: 'var(--text-muted)' }}>Scan your music library to generate stations.</div>}</div> }
