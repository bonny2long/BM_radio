import { useState } from 'react'
type Props={src?:string|null;label:string;size?:number;kind?:'music'|'book'|'station'|'radio';variant?:'square'|'rounded'|'circle'}
const fallback=(label:string,kind:Props['kind'])=>{if(kind==='radio')return'BM';if(kind==='station')return'ST';const clean=label.replace(/^\d+[-\s.]*/,'').split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase();return clean||(kind==='book'?'BK':'BR')}
export default function Artwork({src,label,size=48,kind='music',variant='rounded'}:Props){const [failed,setFailed]=useState(false);const book=kind==='book';const radius=variant==='circle'?'50%':variant==='square'?10:size>100?22:13;return <div style={{width:size,height:book?Math.round(size*1.18):size,flexShrink:0,borderRadius:radius,overflow:'hidden',background:book?'var(--gradient-books)':'var(--gradient-radio)',display:'grid',placeItems:'center',boxShadow:'0 10px 26px rgba(104,82,240,.25)'}}>{src&&!failed?<img src={src} alt="" onError={()=>setFailed(true)} style={{width:'100%',height:'100%',objectFit:'cover'}}/>:<span style={{fontSize:Math.max(13,size*.3),fontWeight:800,letterSpacing:'.04em',color:'#fff'}}>{fallback(label,kind)}</span>}</div>}
