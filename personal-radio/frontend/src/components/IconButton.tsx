import type { ReactNode } from 'react'
export default function IconButton({label,onClick,children,active=false,disabled=false,size=42}:{label:string;onClick?:()=>void;children:ReactNode;active?:boolean;disabled?:boolean;size?:number}){return <button aria-label={label} title={label} onClick={onClick} disabled={disabled} style={{width:size,height:size,borderRadius:'50%',display:'grid',placeItems:'center',background:active?'var(--accent-primary)':'var(--bg-surface)',color:active?'#fff':'var(--text-primary)',border:'1px solid var(--border-subtle)',opacity:disabled?.45:1}}>{children}</button>}
