import React from 'react'
import BottomNav from './BottomNav'
import MiniPlayer from './MiniPlayer'
export default function AppShell({children,currentPage,onPageChange,onOpenNowPlaying}:{children:React.ReactNode;currentPage:string;onPageChange:(page:string)=>void;onOpenNowPlaying:()=>void}){const showNav=currentPage!=='nowPlaying';return <div style={{display:'flex',flexDirection:'column',minHeight:'100vh',paddingBottom:showNav?'164px':'24px',maxWidth:600,margin:'0 auto',width:'100%'}}><main style={{flex:1,padding:'24px 20px 0'}}>{children}</main>{showNav&&<div style={{position:'fixed',bottom:0,left:'50%',transform:'translateX(-50%)',width:'100%',maxWidth:600,zIndex:100}}><MiniPlayer onOpen={onOpenNowPlaying}/><BottomNav currentPage={currentPage} onPageChange={onPageChange}/></div>}</div>}
