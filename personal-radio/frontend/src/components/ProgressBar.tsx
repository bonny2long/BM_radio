import type { CSSProperties } from 'react'
export const formatTime=(seconds:number)=>{if(!Number.isFinite(seconds)||seconds<0)return'0:00';const total=Math.floor(seconds);return `${Math.floor(total/60)}:${String(total%60).padStart(2,'0')}`}
export default function ProgressBar({current,duration,onSeek}:{current:number;duration:number;onSeek:(seconds:number)=>void}){const pct=duration>0?Math.min(100,current/duration*100):0;return <div style={{userSelect:'none'}}><input aria-label="Playback position" type="range" min={0} max={duration||0} value={Math.min(current,duration||0)} onChange={e=>onSeek(Number(e.target.value))} className="seek-range" style={{'--seek-pct':`${pct}%`} as CSSProperties}/><div style={{display:'flex',justifyContent:'space-between',fontSize:11,color:'var(--text-muted)',marginTop:5,fontVariantNumeric:'tabular-nums'}}><span>{formatTime(current)}</span><span>{formatTime(duration)}</span></div></div>}
