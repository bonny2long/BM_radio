import { usePlayback } from '../state/PlaybackContext'
export default function MiniPlayer(){const {nowPlaying,isPlaying,currentTime,duration,togglePlayPause,next}=usePlayback(); const progress=duration?Math.min(100,currentTime/duration*100):0; return <div style={{margin:'0 10px 8px',borderRadius:'var(--radius-l)',background:'rgba(18,18,28,.95)',border:'1px solid var(--border-subtle)',padding:'10px 14px',position:'relative',overflow:'hidden',display:'flex',gap:12,alignItems:'center'}}><div style={{position:'absolute',left:0,bottom:0,width:`${progress}%`,height:2,background:'var(--accent-primary)'}}/><div style={{width:44,height:44,borderRadius:8,background:'var(--gradient-radio)'}}/><div style={{flex:1,minWidth:0}}><strong style={{display:'block',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{nowPlaying?.title ?? 'Not Playing'}</strong><span style={{fontSize:11,color:'var(--text-secondary)'}}>{nowPlaying?.subtitle ?? 'No track selected'}</span></div><button onClick={togglePlayPause} disabled={!nowPlaying} style={{width:40,height:40,borderRadius:'50%',background:'var(--accent-primary)',color:'#fff'}}>{isPlaying?'??':'?'}</button><button onClick={next} disabled={!nowPlaying} style={{width:36,height:36,borderRadius:'50%'}}>?|</button></div>}
