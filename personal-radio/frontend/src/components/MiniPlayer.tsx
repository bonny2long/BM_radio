import Artwork from './Artwork'
import IconButton from './IconButton'
import { NextIcon,PauseIcon,PlayIcon } from './PlayerIcons'
import { usePlayback } from '../state/PlaybackContext'
const QueueIcon=()=> <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="15" y2="12"/><line x1="3" y1="18" x2="12" y2="18"/></svg>
export default function MiniPlayer({onOpen,onOpenQueue}:{onOpen:()=>void;onOpenQueue?:()=>void}){const {nowPlaying,isPlaying,currentTime,duration,togglePlayPause,next,queue,error}=usePlayback();const idle=!nowPlaying;const progress=duration?Math.min(100,currentTime/duration*100):0;const subtitle=idle?'Choose a station, album, or book':nowPlaying.mode==='music'?(nowPlaying.stationName??[nowPlaying.artist,nowPlaying.album].filter(Boolean).join(' \u00b7 ')??nowPlaying.subtitle):(nowPlaying.subtitle||'Audiobook');return <div style={{margin:'0 12px 10px',width:'calc(100% - 24px)',maxWidth:'calc(100% - 24px)',minWidth:0,boxSizing:'border-box',borderRadius:'var(--radius-card)',background:'rgba(18,18,31,.94)',backdropFilter:'blur(22px)',border:'1px solid var(--border-soft)',display:'flex',gap:10,alignItems:'center',padding:10,position:'relative',overflow:'hidden',boxShadow:'var(--shadow-glow)'}}>{error&&<div style={{position:'absolute',top:0,left:0,right:0,background:'rgba(255,80,80,0.15)',borderBottom:'1px solid rgba(255,80,80,0.25)',fontSize:10,color:'#ff8888',textAlign:'center',padding:'2px 8px',zIndex:1}}>{error}</div>}<div style={{position:'absolute',bottom:0,left:0,height:2,width:`${progress}%`,background:'var(--accent-primary)'}}/><button onClick={onOpen} disabled={idle} style={{display:'flex',alignItems:'center',gap:11,flex:'1 1 auto',minWidth:0,textAlign:'left'}}><Artwork src={nowPlaying?.coverUrl} label={nowPlaying?.title??'BM Radio'} size={48} kind={idle?'radio':nowPlaying?.mode==='audiobook'?'book':'music'}/><span style={{minWidth:0}}><strong style={{display:'block',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis',fontSize:13}}>{nowPlaying?.title??'Ready to play'}</strong><span style={{display:'block',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis',color:'var(--text-muted)',fontSize:11}}>{subtitle}</span></span></button><IconButton label={isPlaying?'Pause':'Play'} onClick={togglePlayPause} active={!idle} disabled={idle} size={42}>{isPlaying?<PauseIcon/>:<PlayIcon/>}</IconButton><IconButton label="Next" onClick={next} disabled={queue.length<2} size={36}><NextIcon/></IconButton>{!idle&&onOpenQueue&&<IconButton label="Queue" onClick={onOpenQueue} size={30} variant="ghost"><QueueIcon/></IconButton>}</div>}