import Artwork from './Artwork'
import IconButton from './IconButton'
import { NextIcon, PauseIcon, PlayIcon } from './PlayerIcons'
import { usePlayback } from '../state/PlaybackContext'
export default function MiniPlayer({onOpen}:{onOpen:()=>void}){const {nowPlaying,isPlaying,currentTime,duration,togglePlayPause,next,queue}=usePlayback();const progress=duration?Math.min(100,currentTime/duration*100):0;const idle=!nowPlaying;return <div style={{margin:'0 10px 8px',borderRadius:18,background:'rgba(16,16,28,.97)',border:'1px solid var(--border-subtle)',display:'flex',gap:10,alignItems:'center',padding:10,position:'relative',overflow:'hidden',boxShadow:'0 8px 28px rgba(0,0,0,.4)'}}><div style={{position:'absolute',bottom:0,left:0,height:2,width:`${progress}%`,background:'var(--accent-primary)'}}/><button onClick={onOpen} disabled={idle} style={{display:'flex',alignItems:'center',gap:10,flex:1,minWidth:0,textAlign:'left'}}><Artwork src={nowPlaying?.coverUrl} label={nowPlaying?.title??'BM Radio'} size={48} kind={nowPlaying?.mode==='audiobook'?'book':'music'}/><span style={{minWidth:0}}><strong style={{display:'block',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis',fontSize:13}}>{nowPlaying?.title??'Ready to play'}</strong><span style={{display:'block',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis',color:'var(--text-muted)',fontSize:11}}>{nowPlaying?.subtitle??'Choose a station, album, or book'}</span></span></button><IconButton label={isPlaying?'Pause':'Play'} onClick={togglePlayPause} active={!idle} disabled={idle} size={40}>{isPlaying?<PauseIcon/>:<PlayIcon/>}</IconButton><IconButton label="Next" onClick={next} disabled={queue.length<2} size={36}><NextIcon/></IconButton></div>}
