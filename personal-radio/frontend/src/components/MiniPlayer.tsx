import Artwork from './Artwork'
import IconButton from './IconButton'
import { NextIcon,PauseIcon,PlayIcon } from './PlayerIcons'
import { usePlayback } from '../state/PlaybackContext'
export default function MiniPlayer({onOpen}:{onOpen:()=>void}){const {nowPlaying,isPlaying,currentTime,duration,togglePlayPause,next,queue}=usePlayback();const idle=!nowPlaying;const progress=duration?Math.min(100,currentTime/duration*100):0;const subtitle=idle?'Choose a station, album, or book':nowPlaying.mode==='music'?(nowPlaying.stationName??[nowPlaying.artist,nowPlaying.album].filter(Boolean).join(' - ')??nowPlaying.subtitle):(nowPlaying.subtitle||'Audiobook');return <div style={{margin:'0 12px 10px',width:'calc(100% - 24px)',maxWidth:'calc(100% - 24px)',minWidth:0,boxSizing:'border-box',borderRadius:'var(--radius-card)',background:'rgba(18,18,31,.94)',backdropFilter:'blur(22px)',border:'1px solid var(--border-soft)',display:'flex',gap:10,alignItems:'center',padding:10,position:'relative',overflow:'hidden',boxShadow:'var(--shadow-glow)'}}><div style={{position:'absolute',bottom:0,left:0,height:2,width:`${progress}%`,background:'var(--accent-primary)'}}/><button onClick={onOpen} disabled={idle} style={{display:'flex',alignItems:'center',gap:11,flex:'1 1 auto',minWidth:0,textAlign:'left'}}><Artwork src={nowPlaying?.coverUrl} label={nowPlaying?.title??'BM Radio'} size={48} kind={idle?'radio':nowPlaying?.mode==='audiobook'?'book':'music'}/><span style={{minWidth:0}}><strong style={{display:'block',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis',fontSize:13}}>{nowPlaying?.title??'Ready to play'}</strong><span style={{display:'block',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis',color:'var(--text-muted)',fontSize:11}}>{subtitle}</span></span></button><IconButton label={isPlaying?'Pause':'Play'} onClick={togglePlayPause} active={!idle} disabled={idle} size={42}>{isPlaying?<PauseIcon/>:<PlayIcon/>}</IconButton><IconButton label="Next" onClick={next} disabled={queue.length<2} size={36}><NextIcon/></IconButton></div>}

