# BM Radio backend configuration now lives at:
#
#   backend/.env.example
#
# Copy backend/.env.example to backend/.env and edit values there.
# This root-level file is retained only as a pointer to avoid two competing
# environment contracts.
